Refine the {{.TicketID}} draft using review feedback.

Inputs:
- draft-v1.mdx
- review.md

Task:
- Produce a corrected final article.
- Keep style-guide compliance.
- Ensure frontmatter values remain accurate.
{{if .Angle}}
NARRATIVE ANGLE (preserve through refinement — do NOT flatten back to uniform "cover everything" structure when applying review feedback):

{{.Angle}}

When the review surfaces issues, fix them WITHIN this angle. If a review note suggests adding equal weight to every source section, treat that note as advisory only — angle-pivoted structure is intentional. The opening must still establish the angle within the first 3 paragraphs; the closing must still call back to it.
{{end}}
Output requirements:
- Write final output to final.mdx in the current directory.
- Only use ClawdNote for commentary (no CodexNote/GeminiNote). Add ClawdNotes wherever there's genuine insight — no fixed number, quality over quantity.
- Only import ClawdNote from '../../components/ClawdNote.astro' — no other note components.
