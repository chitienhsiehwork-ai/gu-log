You are translating a finished, tribunal-passed gu-log article from Traditional
Chinese (zh-tw) into English for the en/ sidecar.

The zh-tw article ({{.TicketID}}) has already passed the vibe/quality tribunal — its
structure, MoguNote placement, and narrative are FINAL. Do not restructure it, add
new sections, cut sections, or change its argument. This is a translation task, not a
rewrite.

Translation rules (from GU-LOG_WRITER_PROMPT.md):
- Translate idiomatically, not word-for-word. Read a sentence, understand the idea,
  write the natural English version of that idea.
- Do NOT write like a textbook.
- The en version's quoted source material SHOULD read as the original English (if the
  source was already English, quote it verbatim rather than back-translating a
  zh-tw paraphrase; if the source itself was non-English, translate naturally).
- Preserve every MDX component usage (MoguNote, ShroomDogNote, images, etc.) as-is,
  translating only their text content.
- Keep code blocks, URLs, and technical identifiers unchanged.

Frontmatter changes required for the en version:
- `lang: "en"` (the zh-tw source has `lang: "zh-tw"`)
- `title`, `summary`, and any other free-text frontmatter fields: translate to English
- `ticketId`, `sourceUrl`, `originalDate`, `translatedDate`, `translatedBy`, `tags`,
  `series`, and any other structural/metadata fields: keep IDENTICAL to the zh-tw
  source (do not translate identifiers, dates, or model/harness stamps)

Output requirements:
- Write the translated article to a file named translated-en.mdx in the current
  directory.
- Include valid MDX frontmatter and body.
- Do not leave the output empty.

zh-tw source article ({{.TicketID}}):
{{.Source}}
