# Review: Article Dedup Strategy（歷史快照）

> 狀態：Superseded。這份 2026-04-08 review 只代表當時的 point-in-time 結論，不是現行 dedup contract 或最新驗證證據。

當時 review 引用的 script、line number 與 pipeline 已被刪除或取代。歷史判斷由 git history 保留；本檔不再複述會 drift 的實作細節。

現行 contract 與 runtime 入口統一由 [`Article Dedup Strategy（已取代）`](./article-dedup-strategy.md) 路由。

要評估目前是否符合 contract，必須直接對照上述 OpenSpec 與 code 重新執行測試，不能沿用這份歷史 PASS。
