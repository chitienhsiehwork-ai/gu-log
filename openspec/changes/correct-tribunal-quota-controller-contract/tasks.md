## 1. 政策檢查點與情境對帳

- [ ] 1.1 記錄人類檢查點①：核准以視窗消耗線政策取代逐篇／執行中工作預扣，接受執行中工作不在保留底線保證內，並確認 `--legacy-quota` 不再是正式 operator fallback
- [ ] 1.2 建立差異規格中每個情境對應正式程式路徑與阻擋型迴歸測試的矩陣

## 2. 正式控制器與阻擋型測試

- [ ] 2.1 擴充 `test-tribunal-openai-quota-controller.sh`，直接覆蓋雙視窗消耗線、較長與相同欠額、欠額小於最小冷卻時間、單一及雙視窗保留底線
- [ ] 2.2 補齊執行中工作數與 `ARTICLE_COST_PCT` 改變、但派送決策不變的正式環境錨定迴歸測試
- [ ] 2.3 補齊缺漏／無效資料、用量監測失敗、額外用量安全閥，以及「無效正式來源不得落到有效歷史來源」的正式環境錨定迴歸測試
- [ ] 2.4 把 `--legacy-quota` 限定為非規範歷史相容路徑；正式 OpenAI-only 執行不得把它宣稱成可用 rollback，並同步 CLI／測試／操作手冊邊界
- [ ] 2.5 讓保留底線停止優先於最小冷卻時間，確保重設即將發生時仍不在保留底線以下開新工作行程
- [ ] 2.6 保留 `--controller-once <active_workers>` 相容介面，移除或改寫 `test-quota-controller.sh` 內自我重演舊 Python 公式的假覆蓋；仍有價值的歷史／校準斷言必須改錨正式函式

## 3. 可觀測性與衍生文件

- [ ] 3.1 讓主迴圈 `tick`／`dispatch`／`complete` 重讀失敗時保存明確不可用狀態，而不是 `0|-1` 假量測；校準器拒收任一端不可用、缺欄位或含舊哨兵的配對
- [ ] 3.2 清理 `docs/tribunal-runbook.md` 仍提到預扣重複計算、舊來源快取與單篇成本閘門的除錯殘影
- [ ] 3.3 確認正式執行環境預設值只由程式／設定擁有，操作手冊不複製會偏移的門檻或模型快照

## 4. 驗證與實作審查

- [ ] 4.1 跑額度控制器目標測試、OpenSpec 嚴格驗證、情境到測試對帳與規格／實作分離閘門
- [ ] 4.2 跑 repo 要求的 lint、型別檢查、測試、內容驗證、建置、提交前與推送前閘門
- [ ] 4.3 完成安全性／正確性實作審查，修完所有阻擋發現
- [ ] 4.4 完成 Keep／Simplify／Drop 實作審查，修完所有阻擋發現

## 5. 歸檔與發佈檢查點

- [ ] 5.1 在同一 PR 歸檔 change，確認穩定版規格的 Requirements 與 Purpose 都不再宣稱逐篇或執行中工作預扣
- [ ] 5.2 對歸檔後 repo 重跑 OpenSpec 嚴格驗證、必要 CI 與正式環境錨定額度測試
- [ ] 5.3 記錄人類檢查點②：核准歸檔後全貌，才把 draft PR 轉為 ready
- [ ] 5.4 CI 全綠後合併，驗證 main、正式環境部署與 smoke；若外部部署阻擋持續，留下耐久檢查點
