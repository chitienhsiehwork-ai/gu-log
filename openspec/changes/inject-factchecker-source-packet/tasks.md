## 1. 安全來源擷取

- [ ] 1.1 在既有來源擷取器的網路層補公開目的地、DNS 綁定、重新導向、含憑證網址與讀取／逾時上限檢查，不新增無防護的備援路徑。
- [ ] 1.2 保留既有文字抽取與來源驗證器，以原子 sidecar／型別化觀察提供 X、一般文章與 YouTube 身分；將 pinned yt-dlp Python package 接上既有 requirements／CI 安裝入口，驗證唯一安全 handler。
- [ ] 1.3 新增不連外的傳輸／來源身分／完整性回歸測試，覆蓋公開及內網重新導向、DNS 指向內網、來源不符、超限、預覽文字、缺字幕與完整串文。

## 2. Packet 與 runner

- [ ] 2.1 建立私有來源封裝工具，包含資訊清單、一般檔案的路徑限制、位元組數／SHA-256、來源身分、完整性驗證與明確重播入口。
- [ ] 2.2 GP／MP FactChecker 在評審迴圈前取得來源，重試沿用相同資料；來源失敗記為操作性錯誤，前置檢查的評審次數為零，不呼叫 judge、不增加內容嘗試次數。
- [ ] 2.3 接上 Codex／Claude 評審工作目錄的唯讀資料傳遞與前後完整性檢查，由 parent 在 FactChecker stage ledger 寫入經驗證的 sourceEvidence，writer 不得修改來源封裝。
- [ ] 2.4 更新 FactChecker 契約，指定來源是不可信的證據資料，保留 GP／MP 編輯差異及 SD／Lv 的無條件查核。

## 3. 整合驗收

- [ ] 3.1 新增完全隔離的模擬擷取器／評審測試，證明完整 X 與一般來源在無網路評審環境仍可讀取，來源失敗不產生文章品質 FAIL。
- [ ] 3.2 測試提示注入文字、路徑越界、符號連結、雜湊竄改、重試只擷取一次，以及明確重播不需網路，逐項對應規格情境。
- [ ] 3.3 在既有測試歸屬與可攜式 shell 橋接登記新測試，執行來源、Tribunal 與既有來源保存回歸測試，以及必要建置／內容檢查。

## 4. 審查與發布準備

- [ ] 4.1 通過獨立正確性／安全與 Keep/Simplify/Drop 審查，修正所有阻擋問題。
- [ ] 4.2 驗證預覽環境，或為純工具變更提供等價的測試證據，完成 OpenSpec 同步／封存與必要 hooks。

PR 轉為可審查狀態、GitHub 審查、CI、合併、正式環境冒煙測試，以及保留 operator stop 的執行環境檢查，由 controller 按 SDLC 在封存後接續；這些交付責任不可用封存核取方塊宣稱已完成。
