# 繁體中文寫作風格指南（gu-log）

Sub-agent 寫文章前**必讀**此檔案。這是踩過坑累積出來的規則。

---

## 簡繁差異（最常見地雷）

| ❌ 簡體/大陸用法 | ✅ 台灣繁體用法 | 備註 |
|:--|:--|:--|
| 水平（指程度） | **水準** | 「水平」只用於 horizontal（如：水平基因轉移） |
| 信息 | **資訊** | |
| 視頻 | **影片** | |
| 軟件 | **軟體** | |
| 硬件 | **硬體** | |
| 數據庫 | **資料庫** | |
| 數據 | **資料**（或保留「數據」看語境） | benchmark 數據可接受 |
| 人工智能 | **人工智慧** | 或直接寫 AI |
| 網絡 | **網路** | |
| 服務器 | **伺服器** | |
| 內存 | **記憶體** | |
| 程序 | **程式** | |
| 算法 | **演算法** | |
| 代碼 | **程式碼** | 或直接寫 code |
| 默認 | **預設** | |
| 優化 | **最佳化**（或「優化」皆可） | 台灣兩者都用，「優化」已通用 |
| 調用 | **呼叫** | API call = API 呼叫 |
| 博客 | **部落格** | |
| 鏈接 | **連結** | |
| 上線 | **上線**（OK）或 **部署** | |
| 支持 | **支援** | |
| 實現 | **實作**（動詞）/ **實現**（名詞皆可） | |
| 用戶 | **使用者** | 「用戶」在台灣也通用，但「使用者」更正式 |
| 交互 | **互動** | |

## 格式規則

- **不用 markdown table** — 手機上會爆，一律用 bullet list
- **專有名詞保留英文** — benchmark, model, token, context window 等不翻
- **Kaomoji 不要含 markdown 特殊字元**（backticks, asterisks, underscores）
- **標題要有梗** — PTT 說故事風，不是學術論文

## 語氣風格

- **PTT 說故事風 + 李宏毅教授風格** — 輕鬆但有深度
- **加 Clawd 註解吐槽** — 用 `<ClawdNote>` component
- **不要太正式** — 像在跟朋友聊天，但聊的是有料的東西
- **適度使用口語** — 「超猛」「很扯」「根本」「直接」這些都 OK

## 常見錯誤提醒

- 「的地得」不要混用（雖然台灣口語常混，但書面盡量正確）
- 中英文之間加空格（如：這個 benchmark 很猛）
- 數字和單位之間加空格（如：72.5%，不是 72.5 %）
- 百分比用半形 %

---

*這份指南會持續更新。每次踩到新坑就加進來。*
