---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import Toggle from '../components/Toggle.astro';

// Get all zh-tw posts sorted by date (newest first)
const allPosts = (await getCollection('posts', ({ data }) => {
  return data.lang === 'zh-tw';
})).sort((a, b) => b.data.date.localeCompare(a.data.date));

// Split: regular articles vs Clawd Picks
const posts = allPosts.filter(p => !p.data.tags?.includes('clawd-picks'));
const picks = allPosts.filter(p => p.data.tags?.includes('clawd-picks'));
---

<BaseLayout title="ShroomDog">
  <h1><img src="/avatar.jpg" alt="香菇大狗狗" class="site-avatar" /> 香菇大狗狗</h1>
  <p class="intro">
    精選外文好文，翻譯成繁體中文。每篇都附原文連結。
  </p>
  <p class="author-note">
    by ShroomDog and Clawd
  </p>

  <section class="posts-section">
    <h2>最新文章</h2>

    {posts.map((post) => (
      <article class="post-preview">
        <h3><a href={`/posts/${post.slug}`}>{post.data.title}</a></h3>
        <p class="post-meta">{post.data.date} · 翻譯自 {post.data.source}</p>

        <Toggle title="摘要預覽">
          <p>{post.data.summary}</p>
        </Toggle>
      </article>
    ))}
  </section>

  {picks.length > 0 && (
    <section class="clawd-picks-section" id="clawd-picks">
      <h2><img src="/clawd-icon.png" alt="" class="clawd-section-icon" /> Clawd Picks</h2>
      <p class="clawd-picks-subtitle">Clawd 每小時精選一則推文翻譯</p>

      {picks.slice(0, 10).map((pick) => (
        <article class="pick-preview">
          <a href={`/posts/${pick.slug}`}>{pick.data.title}</a>
          <span class="pick-meta">{pick.data.date} · {pick.data.source}</span>
        </article>
      ))}

      {picks.length > 10 && (
        <Toggle title={`查看更多（共 ${picks.length} 則）`}>
          {picks.slice(10).map((pick) => (
            <article class="pick-preview">
              <a href={`/posts/${pick.slug}`}>{pick.data.title}</a>
              <span class="pick-meta">{pick.data.date} · {pick.data.source}</span>
            </article>
          ))}
        </Toggle>
      )}
    </section>
  )}

  <section class="about-section">
    <h2>關於這裡</h2>

    <Toggle title="什麼是這個網站？">
      <p>這是一個翻譯 blog，把有趣的外文文章翻成繁體中文分享給大家。</p>
    </Toggle>

    <Toggle title="為什麼做這個？">
      <p>因為好內容不應該有語言障礙。而且翻譯的過程也是學習的過程。</p>
    </Toggle>

    <Toggle title="技術細節（給工程師看的）" open={false}>
      <p>這個網站使用：</p>
      <ul>
        <li>Astro - 靜態網站生成器</li>
        <li>Claude Opus 4.5 - AI 翻譯協助</li>
        <li>人工校對 - 確保品質</li>
      </ul>
    </Toggle>
  </section>

  <section class="theme-demo">
    <h2>主題切換</h2>
    <p>點右上角的切換按鈕切換深色/淺色（Solarized Light）模式</p>
  </section>
</BaseLayout>

<style>
  .site-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    vertical-align: middle;
    margin-right: 0.5rem;
  }

  .intro {
    color: var(--color-text-muted);
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }

  .author-note {
    color: var(--color-accent);
    font-size: 0.9rem;
    margin-bottom: 2rem;
    font-style: italic;
  }

  .posts-section, .about-section, .theme-demo, .clawd-picks-section {
    margin-bottom: 2rem;
  }

  .clawd-section-icon {
    width: 24px;
    height: 24px;
    vertical-align: middle;
  }

  .clawd-picks-subtitle {
    color: var(--color-text-muted);
    font-size: 0.85rem;
    margin-bottom: 1rem;
  }

  .pick-preview {
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--color-border);
    font-size: 0.9rem;
  }

  .pick-preview a {
    color: var(--color-text);
  }

  .pick-preview a:hover {
    color: var(--color-accent);
  }

  .pick-meta {
    display: block;
    color: var(--color-text-muted);
    font-size: 0.75rem;
  }

  .post-preview {
    padding: 1rem 0;
    border-bottom: 1px solid var(--color-border);
  }

  .post-preview h3 {
    margin-bottom: 0.25rem;
  }

  .post-preview h3 a {
    color: var(--color-text);
  }

  .post-preview h3 a:hover {
    color: var(--color-accent);
  }
</style>
