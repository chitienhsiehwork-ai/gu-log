---
/**
 * TicketBadge - Display article ticket ID with appropriate styling
 *
 * Types:
 * - SD: ShroomDog Original (accent color)
 * - GP: Gu-log Picks (muted blue)
 * - MP: Mogu Picks (mogu orange)
 * - Lv: Level-Up (purple)
 */
interface Props {
  ticketId: string;
  showTooltip?: boolean;
  showLabel?: boolean;
}

const { ticketId, showTooltip = true, showLabel = false } = Astro.props;

// Extract prefix (SD, GP, MP, Lv)
const prefix = ticketId.split('-')[0];

const labels: Record<string, string> = {
  SD: 'ShroomDog Original',
  GP: 'Gu-log Picks',
  MP: 'Mogu Picks',
  Lv: 'Level-Up',
};

const label = labels[prefix] || '';
---

<span class="ticket-wrapper">
  <span
    class={`ticket-badge ticket-${prefix.toLowerCase()}`}
    title={showTooltip && !showLabel ? label : undefined}
  >
    {ticketId}
  </span>
  {showLabel && <span class={`ticket-label ticket-label-${prefix.toLowerCase()}`}>{label}</span>}
</span>

<style>
  .ticket-wrapper {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .ticket-badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.1rem 0.4rem;
    border-radius: 3px;
    font-family: 'SF Mono', 'Fira Code', monospace;
    letter-spacing: 0.03em;
    vertical-align: middle;
    cursor: default;
  }

  .ticket-label {
    font-size: 0.85rem;
    font-weight: 500;
  }

  /* SD: ShroomDog Original - muted green/teal */
  .ticket-sd {
    background-color: var(--color-badge-sd-bg);
    color: var(--color-badge-sd);
    border: 1px solid var(--color-badge-sd-border, var(--color-badge-border));
  }

  .ticket-label-sd {
    color: var(--color-badge-sd);
  }

  /* GP: Gu-log Picks - muted blue */
  .ticket-gp {
    background-color: var(--color-badge-gp-bg);
    color: var(--color-badge-gp);
    border: 1px solid var(--color-badge-gp-border, var(--color-badge-border));
  }

  .ticket-label-gp {
    color: var(--color-badge-gp);
  }

  /* MP: Mogu Picks - muted orange */
  .ticket-mp {
    background-color: var(--color-badge-mp-bg);
    color: var(--color-badge-mp);
    border: 1px solid var(--color-badge-mp-border, var(--color-badge-border));
  }

  .ticket-label-mp {
    color: var(--color-badge-mp);
  }

  /* Lv: Level-Up - muted purple */
  .ticket-lv {
    background-color: var(--color-badge-lv-bg);
    color: var(--color-badge-lv);
    border: 1px solid var(--color-badge-lv-border, var(--color-badge-border));
  }

  .ticket-label-lv {
    color: var(--color-badge-lv);
  }
</style>
