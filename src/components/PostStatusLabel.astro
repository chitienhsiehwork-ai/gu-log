---
import type { PostStatus } from '../utils/post-status';

interface Props {
  status: Exclude<PostStatus, 'published'>;
}

const { status } = Astro.props;
const label = `[${status}]`;
---

<span class={`post-status-label post-status-label--${status}`}>{label}</span>
