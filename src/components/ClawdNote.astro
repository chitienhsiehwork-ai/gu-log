---
// Legacy alias. New content should import MoguNote directly.
import MoguNote from './MoguNote.astro';

interface Props {
  prefix?: string;
  summary?: string;
  variant?: 'note' | 'murmur';
}

const props = Astro.props as Props;
---

<MoguNote {...props}>
  <slot />
</MoguNote>
